/*
An ARK button represents a function.

It is a button with several "tails" that can be connected to other objects.
It can stick to any object that understands it.

When clicked, the button evaluates the function with
*/

const ARKButton = {

}